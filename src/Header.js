import React from "react";
const Header = () => (
	<div>
		<h1 className="weatherHeading">My Weather App</h1>
		<p className="weatherHeading">To get weather for a particular city, enter details below</p>
	</div>
);

export default Header;